# 🦞 交易记录优化方案

**创建时间：** 2026-03-10 12:00  
**状态：** ✅ API 已优化，前端待更新  

---

## 📊 优化内容

### 1. 交易记录类型标注

**新增字段：** `trade_type`

**可选值：**
- `strategy_open` - 策略开仓
- `strategy_add` - 策略加仓
- `strategy_close` - 策略平仓
- `manual_open` - 手动开仓
- `manual_close` - 手动平仓

**前端显示：**
```
[策略] ETHUSDT OPEN    ← trade_type = strategy_open
[策略] ETHUSDT ADD     ← trade_type = strategy_add
[策略] ETHUSDT CLOSE   ← trade_type = strategy_close
[手动] ETHUSDT OPEN    ← trade_type = manual_open
```

---

### 2. 详细信息字段

**新增字段：** `details`

**包含信息：**
```json
{
  "leverage": 2,              // 杠杆倍数
  "position_value": 1000.00,  // 仓位价值
  "margin": 500.00,           // 保证金
  "stop_loss_price": 2000.00, // 止损价格
  "stop_order_id": 12345      // 止损单 ID
}
```

**前端详情弹窗：**
```
交易详情:
├─ 交易对：ETHUSDT
├─ 方向：LONG
├─ 数量：0.5
├─ 价格：$2000
├─ 杠杆：2x
├─ 仓位价值：$2000
├─ 保证金：$1000
├─ 止损价：$1900
└─ 订单 ID: 12345
```

---

### 3. API 优化

**已修改文件：** `api/trading_record.py`

**修改内容：**
```python
def create_trade_record(...):
    # 新增参数
    trade_type: str = None,      # 交易类型
    details: Dict = None         # 详细信息

# 自动判断交易类型
if trade_type is None:
    if strategy and strategy != 'manual':
        if action == 'OPEN':
            trade_type = 'strategy_open'
        elif action == 'ADD':
            trade_type = 'strategy_add'
        else:
            trade_type = 'strategy_close'
    else:
        trade_type = 'manual_open' if action == 'OPEN' else 'manual_close'
```

---

## 🎨 前端显示优化

### 交易记录列表

**当前显示：**
```
时间 | 交易对 | 方向 | 数量 | 价格 | 盈亏
```

**优化后：**
```
时间 | 类型 | 交易对 | 方向 | 数量 | 价格 | 盈亏 | 详情
-----|------|--------|------|------|------|------|------
11:30 | [策略] | ETHUSDT | LONG | 0.5 | $2000 | - | 🔍
11:31 | [策略] | ETHUSDT | LONG | 0.3 | $2001 | - | 🔍
11:32 | [策略] | ETHUSDT | CLOSE| 0.8 | $2005 | +$4 | 🔍
```

### 详情弹窗

**点击"🔍"显示：**
```
┌─────────────────────────────────┐
│ 交易详情                        │
├─────────────────────────────────┤
│ 交易 ID: 1                      │
│ 时间：2026-03-10 11:30:00      │
│ 交易对：ETHUSDT                 │
│ 方向：LONG                      │
│ 类型：策略开仓                  │
│ 策略：auto_sim                  │
│                                 │
│ 数量：0.5 ETH                   │
│ 价格：$2000                     │
│ 金额：$1000                     │
│                                 │
│ 杠杆：2x                        │
│ 仓位价值：$2000                 │
│ 保证金：$1000                   │
│                                 │
│ 止损价：$1900                   │
│ 止损单 ID: 12345                │
│                                 │
│ 状态：已成交                     │
└─────────────────────────────────┘
```

---

## 📋 实施步骤

### 已完成
- ✅ API 优化（trading_record.py）
- ✅ 新增 trade_type 字段
- ✅ 新增 details 字段

### 待执行
- ⏳ 网关调用优化（gateway.py）
- ⏳ 前端显示优化（testnet.html）
- ⏳ 详情弹窗组件

---

## 🔧 网关调用示例

```python
# gateway.py - 策略启动时
trade = trading_record.create_trade_record(
    symbol=symbol,
    side=side.upper(),
    action='OPEN',
    quantity=quantity,
    price=entry_price,
    strategy=strategy_name,
    signal_reason='策略启动',
    status='executed',
    stop_loss_price=stop_price,
    stop_order_id=stop_order_id,
    trade_type='strategy_open',  # ← 新增
    details={                    # ← 新增
        'leverage': leverage,
        'position_value': position_value,
        'margin': margin,
        'stop_loss_price': stop_price,
        'stop_order_id': stop_order_id
    }
)
```

---

## 🎯 预期效果

**用户体验提升：**
- ✅ 一眼看出是策略交易还是手动交易
- ✅ 点击详情看到完整信息
- ✅ 包含杠杆、保证金、止损等关键数据

**便于分析：**
- ✅ 可以按交易类型筛选
- ✅ 可以统计策略表现
- ✅ 可以追踪止损单执行情况

---

## 📝 备注

**优先级：** ⚠️ 中（用户体验优化）  
**预计耗时：** 2 小时（前端部分）  
**状态：** API 已完成，前端待开发  

---

**🦞 交易记录优化方案已制定！**
